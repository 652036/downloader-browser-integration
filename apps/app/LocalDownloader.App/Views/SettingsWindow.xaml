<Window x:Class="LocalDownloader.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="480" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Save directory:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,8,8"
                 Text="{Binding DownloadDirectory, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="0" Grid.Column="2" Content="Browse…" Padding="8,2" Margin="0,0,0,8"
                Command="{Binding BrowseDownloadDirectoryCommand}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Connections per task:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" HorizontalAlignment="Left" Width="80"
                 Text="{Binding ConnectionsPerTask, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Concurrent tasks:" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,8" HorizontalAlignment="Left" Width="80"
                 Text="{Binding MaxConcurrentTasks, UpdateSourceTrigger=PropertyChanged}" />

        <CheckBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Content="Launch at Windows startup" Margin="0,0,0,8"
                  IsChecked="{Binding LaunchAtStartup}" />

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Text="Intercepted file extensions (one per line):" Margin="0,0,0,4" />
        <TextBox Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,8"
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap"
                 Text="{Binding InterceptExtensionsText, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="10,4" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
            <Button Content="Save" Padding="10,4" IsDefault="True" Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</Window>
